<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:clipChildren="false">

    <!-- Premium Status Bubble (Glassmorphism effect) -->
    <androidx.cardview.widget.CardView
        android:id="@+id/status_bubble"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="16dp"
        android:visibility="gone"
        app:cardBackgroundColor="#AA0A0A0A"
        app:cardCornerRadius="24dp"
        app:cardElevation="8dp"
        app:layout_constraintBottom_toBottomOf="@id/jarvis_orb"
        app:layout_constraintEnd_toStartOf="@id/jarvis_orb"
        app:layout_constraintTop_toTopOf="@id/jarvis_orb">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:paddingHorizontal="20dp"
            android:paddingVertical="12dp"
            android:background="@drawable/bg_status_pill">

            <TextView
                android:id="@+id/status_text"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Thinking..."
                android:textColor="#E0FFFFFF"
                android:textSize="14sp"
                android:letterSpacing="0.02"
                android:fontFamily="sans-serif-medium" />
        </LinearLayout>
    </androidx.cardview.widget.CardView>

    <com.jarvisai.app.ui.views.JarvisOrbView
        android:id="@+id/jarvis_orb"
        android:layout_width="96dp"
        android:layout_height="96dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_margin="-12dp" /> <!-- Negative margin to account for glow -->


</androidx.constraintlayout.widget.ConstraintLayout>
